# Rebuilt from the actively used conda env: zip
# Goal: keep install behavior close to the current working setup,
# while avoiding unrelated packages from a full environment freeze.

# PyTorch CUDA 12.6 wheels
--extra-index-url https://download.pytorch.org/whl/cu126

# Core runtime
torch==2.11.0+cu126
transformers==5.3.0
tokenizers==0.22.2
safetensors==0.7.0
sentencepiece==0.2.1
huggingface_hub==1.7.2
hf-xet==1.4.2

# Dataset / benchmark / download path
datasets==4.8.4
numpy==2.2.6
pandas==2.3.3
pyarrow==23.0.1
requests==2.32.5
httpx==0.28.1
aiohttp==3.13.3
dill==0.4.1
multiprocess==0.70.19
xxhash==3.6.0

# Shared runtime utilities required by the pinned stack
filelock==3.25.2
fsspec==2026.2.0
packaging==25.0
PyYAML==6.0.3
regex==2026.2.28
tqdm==4.67.3
typer==0.24.1
